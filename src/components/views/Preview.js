import React from "react";
import Container from "@mui/material/Container";

const Preview = () => {
  return (
    <Container
      sx={{
        bgcolor: "#EEEEEE",
        padding: "50px",
        marginTop: "50px",
        marginBottom: "50px",
        borderRadius: "10px",
      }}
    ></Container>
  );
};

export default Preview;
